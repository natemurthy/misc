"""
brute force
"""
def method(arr):
    n = len(arr)
    for i in range(n-1):
        for j in range(i+1, n):
            # do stuff
    return


"""
left-right search
"""
def lr_search(lr):
    l, r = 0, len(arr)-1
    while l < r:
        # do something
        l += 1 # maybe under some condition
        r -= 1 # maybe under alternate condition
    return


"""
spiral matrix
"""

