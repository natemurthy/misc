[pytest]
testpaths = tests
markers =
    slow: end-to-end tests that take more than a few seconds (none here yet; run with -m slow)
    fast: everything else; applied automatically to any test not marked slow (run with -m fast)
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
