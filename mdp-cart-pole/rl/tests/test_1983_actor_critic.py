"""Tests for the 1983 ASE/ACE actor-critic on the BOXES decoder (Barto, Sutton and Anderson): the shared agent interface."""

from interface_checks import AgentInterfaceTests


class TestInterface(AgentInterfaceTests):
    solution = "1983_actor_critic"
