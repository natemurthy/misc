"""Tests for the 1986 actor-critic with backprop networks (Anderson): the shared agent interface."""

from interface_checks import AgentInterfaceTests


class TestInterface(AgentInterfaceTests):
    solution = "1986_actor_critic_backprop"
